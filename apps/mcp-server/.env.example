# ═══════════════════════════════════════
# MiroThinker MCP Server 环境变量
# ═══════════════════════════════════════

# ── 必填: 搜索 ──
SERPER_API_KEY=your_serper_key_here
# SERPER_BASE_URL=https://google.serper.dev    # 通常不需要改

# ── 必填: 网页抓取 ──
JINA_API_KEY=your_jina_key_here
# JINA_BASE_URL=https://r.jina.ai              # 通常不需要改

# ── 必填: 主 LLM（用于 miro_research 的推理）──
LLM_API_KEY=your_llm_api_key_here
LLM_BASE_URL=https://api.deepseek.com/v1       # OpenAI SDK 格式（不含 /chat/completions）
LLM_MODEL=deepseek-chat

# ── 可选: 摘要 LLM（用于 miro_read 的内容提取）──
# 不填则复用主 LLM。建议用小模型节省成本
# 支持两种 URL 格式:
#   格式1 (OpenAI SDK): https://api.xxx.com/v1
#   格式2 (直连，兼容原项目): https://api.xxx.com/v1/chat/completions
# SUMMARY_LLM_API_KEY=
# SUMMARY_LLM_BASE_URL=
# SUMMARY_LLM_MODEL=
